meta {
  name: Handle Approval Decision
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/service-order/approval/{{approvalToken}}
  body: json
  auth: inherit
}

body:json {
  {
    "decision": "{{approvalDecision}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
