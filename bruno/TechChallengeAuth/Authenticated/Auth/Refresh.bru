meta {
  name: Refresh
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/auth/refresh
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}

script:post-response {
  bru.setEnvVar('token', res.getBody().access_token)
}
