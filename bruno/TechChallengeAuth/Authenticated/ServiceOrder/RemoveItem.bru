meta {
  name: Remove Item From Service Order
  type: http
  seq: 12
}

delete {
  url: {{baseUrl}}/service-order/{{serviceOrderId}}/items/{{itemId}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
