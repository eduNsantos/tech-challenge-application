apiVersion: v1
kind: Secret
metadata:
  name: app-secret
  namespace: postech
type: Opaque
# Todos os valores devem ser codificados em base64.
# Para gerar: echo -n "valor" | base64
# Para o APP_KEY, use: php artisan key:generate --show | base64
# Para o JWT_SECRET, use: php artisan jwt:secret --show | base64
stringData:
  APP_KEY: "base64:SUBSTITUA_PELA_CHAVE_GERADA_COM_php_artisan_key_generate"
  DB_PASSWORD: "123123"
  JWT_SECRET: "SUBSTITUA_PELO_SECRET_GERADO_COM_php_artisan_jwt_secret"
  MAIL_USERNAME: "evdwsoat15@gmail.com"
  MAIL_PASSWORD: "SUBSTITUA_PELA_SENHA_DO_EMAIL"
